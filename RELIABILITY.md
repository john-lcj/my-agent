# 可靠性 · 为什么"没人盯也敢信"

把它放上工作电脑、让它后台替你干活,凭什么信得过?靠的不是"模型很聪明",而是下面这几道**不依赖模型自觉**的机制。它们大多由代码强制,模型想绕也绕不过。

## 1. 硬边界(代码层拒绝,模型无权覆盖)
`governance/policy.yaml` + `governance/engine.py`:`rm -rf`、`mkfs`、强推主干、`curl|sh`、读 `.env`/`.ssh`/私钥等,命中即 **BLOCK**。这不是提示词劝阻,是执行前拦截。

## 2. 工作区范围限制(P0)
设了 `AGENT_WORKSPACE_ROOT` 后,`fs.*` 碰工作区外的路径就需确认(STRICT 模式直接拒)。且这道检查在"本任务已授权"之前——**即便被提示注入、即便开了全自动,也逃不出工作区**。

## 3. 防注入外发(P0)
外部内容(邮件/网页)只当数据不当指令;外发邮件收件人限白名单(默认只发给自己)。读到坏指令也送不出机密。

## 4. 证据铁律(防编造)
事实/数字必须来自工具的真实执行,回复要附命令与原始输出。光给结论不亮证据会被判不可信——这条写进了系统提示与执行档 worker 的交付要求。

## 5. 验证/返修回路(Reflexion)
DAG 子任务带"验收标准",`LLMVerifier` 核验产物;不达标自动返修,有界重规划。不是"跑完就交"。

## 6. 审计日志(可追溯)
每次能力调用追加到 `logs/audit.log`(时间/能力/决策/成败,只记安全字段不记内容)。事后能复盘"它到底做了什么"。

## 7. 可还原快照 + 资源锁
写文件前做快照(可 `/rollback`);并行写同一文件串行化,避免互相覆盖。

## 8. 回归 + 真实评测(持续验证)
- **回归**(每次改动必跑,确定性):`python -m pytest -q tests/` —— 当前 60+ 项,锁住治理/记忆/编排/渠道/动态子代理等关键行为不退化。
- **真实评测**(每周,LLM-as-judge):`python -m eval.run_real --model deepseek-v4-pro`,用例在 `eval/personal/tasks.yaml`,报告存 `logs/eval_reports/`,与上次对比判断"改动让它变聪明还是变笨"。新增了 research-deliver / parallel-summarize 两例,覆盖动态子代理路径。

## 怎么自查
- 看它最近做了什么:`tail -f logs/audit.log`
- 服务是否在跑:浏览器开 `http://localhost:8000/healthz`
- 改了代码后:先 `pytest`,再(有真实 key 时)`eval.run_real`,对比报告。

> 诚实的边界:这些保证的是"行为可控、可追溯、不乱来",**不等于**模型永远正确——推理能力的天花板在底层模型。所以高价值/不可逆操作仍建议留确认,别全自动。
