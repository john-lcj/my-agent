# 单 agent 闭环(MockLLM)零依赖即可运行。
# 下面是按需启用的可选依赖:

# 真实模型 provider(用哪个装哪个):
openai>=1.30.0          # OpenAI 及 DeepSeek(OpenAI 兼容协议)
anthropic>=0.34.0       # Claude

# 声明式策略 YAML(不装则回退到内置安全默认):
PyYAML>=6.0

# 向量记忆(RAG):
sqlite-vec>=0.1.9
numpy>=1.24.0

# Token 计数(P1B):
tiktoken>=0.7.0

# GUI 控制(P3):
# macOS: 零额外依赖(screencapture + osascript 系统内置)
# 如需 Windows/Linux 可选装 pyautogui>=0.9.54

# 联网与邮件:
aiohttp>=3.9.0          # web.search / web.fetch
aiosmtplib>=3.0.0       # 异步 SMTP(可选;邮件渠道默认用 stdlib smtplib)
aioimaplib>=1.1.0       # 异步 IMAP(可选;邮件渠道默认用 stdlib imaplib)

# CLI 交互(斜杠补全菜单):
prompt_toolkit>=3.0.0

# 聊天界面(Web)服务:
fastapi>=0.110.0
uvicorn>=0.29.0
websockets>=12.0

# 回归测试:
pytest>=8.0.0

# 想要可复现的精确锁定:在跑通的环境里执行
#   .venv/bin/pip freeze > requirements.lock.txt
