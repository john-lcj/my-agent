# 手机直连 Captain(经 Tailscale,随时随地)

不接 QQ/微信,直接在手机浏览器里打开你电脑上那个聊天网页——这是你自己的服务,没有第三方、不封号、不限流、实时。Tailscale 负责把手机和电脑拉进一个**私有网**,在外面用流量也能连,而且不暴露公网。

电脑端的配置我已经写进 `.env`(绑定 0.0.0.0 + 访问密码),你只需装 Tailscale。

## 一次性设置

**1. 电脑装 Tailscale 并登录**
- 下载:https://tailscale.com/download (macOS)
- 装好后用同一个账号登录(Google/微软/邮箱都行)
- 登录后,菜单栏 Tailscale 图标里能看到本机的地址,形如 `100.x.x.x`(记下它)

**2. 手机装 Tailscale 并登录**
- App Store 搜 Tailscale,装好,用**同一个账号**登录
- 登录后手机就和电脑在同一个私有网里了

**3. 电脑启动服务(用新配置)**
```
lsof -ti tcp:8000 | xargs kill -9
cd ~/Desktop/"my agent" && .venv/bin/myagent-web
```
启动日志应显示 `Web 聊天 → http://0.0.0.0:8000`。

**4. 手机打开**
手机浏览器访问(把 100.x.x.x 换成第 1 步记下的电脑 Tailscale 地址):
```
http://100.x.x.x:8000
```
首次进去先打开「设置 → 访问令牌(远程访问用)」,填入 `.env` 里那串 `AGENT_API_TOKEN`,保存。
(令牌是机密,只在你的 `.env` 里,别贴到聊天/仓库。)填一次,浏览器记住,以后直接进。

## 之后怎么用
- 电脑开着、`myagent-web` 跑着,手机随时随地打开那个网址就能聊。
- 想更顺手:在手机浏览器里把它「添加到主屏幕」,就像个 App 图标。

## 说明
- **安全**:服务虽绑 0.0.0.0,但只有装了 Tailscale、登录你账号的设备进得了那个私有网;再加 `AGENT_API_TOKEN` 一道密码。不接 Tailscale 的人即使同 WiFi 也连不上控制面(/api 和 /ws 都校验 token)。
- 电脑睡眠/关机时手机会连不上(agent 在电脑上干活,电脑得醒着)。可在 macOS 设置里让它接电源时不休眠。
- 想换密码:改 `.env` 的 `AGENT_API_TOKEN`,重启,手机重新输一次。
