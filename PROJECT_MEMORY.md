# Captain 项目长期记忆

> 这份文件给未来继续参与 Captain 的人和 agent 读。先读这里,再看 README / ARCHITECTURE / CONTEXT。  
> 它记录的不是某一次改动,而是这个项目真正想靠近的方向。

## 北极星

Captain 的目标不是做一个“什么都会一点”的超级聊天框,而是做一个能在模糊目标里可靠推进事情的私人协作者。

更短的一句话:

**Captain 的目标不是万能,而是可靠推进。**

它应该让用户感觉:我不是在指挥一个工具,而是在和一个很聪明、很可靠、会主动补位的伙伴一起工作。

我们知道现阶段做不到真正 AGI,但项目方向要尽量向这个形态靠近:能陪用户思考,也能进入工作现场把事情做完;能聊战略,也能改文件;能执行,也能在执行前讲清风险。

## 产品判断

通用型 agent 的核心不在“知识量”或“工具数量”,而在以下几层能力叠加:

1. **理解语境**
   用户说“帮我看看这个”时,Captain 要判断用户真正需要的是审阅、修复、解释、总结、决策建议,还是只是一起理清思路。它要先判断此刻该扮演什么角色。

2. **自主拆解**
   用户不应被迫写完美工单。Captain 要能自己补齐合理假设,先看环境、读资料、跑验证、形成判断;只有当答案会真正改变方向时才问用户。

3. **自然使用工具**
   搜索、代码执行、文件编辑、浏览器、日历、邮件、数据库、设计工具都只是“手”。工具调用不应炫技,而要服务于判断:什么时候查证,什么时候动手,什么时候停止并确认。

4. **持续上下文**
   Captain 不是一次性问答机器。它要记住项目背景、用户偏好、已做过的事、历史决策和被验证有效的方法。没有长期上下文,它就只是每次重新培训的临时工。

5. **边界感和可信度**
   越通用,越需要诚实。Captain 要知道什么时候不确定、什么时候需要引用来源、什么时候不能替用户做决定。强 agent 不该装作全知,而要让用户信任它的判断过程。

6. **工作现场能力**
   最终价值不是说得好听,而是把真实事情推进:改文件、跑命令、做网页、写报告、发邮件、检查结果、交付产物,并能解释风险和下一步。

## 当前产品定位

Captain 是本地优先的私人 AI Agent:

- 数据和密钥尽量留在用户自己的机器上。
- Web UI 是主要入口,手机远程访问通过 Tailscale / token 保护。
- 单 agent 顺序执行是当前主线,用待办清单推进,不要为了“多 agent 感”制造复杂度。
- 安全是最大卖点之一:本地化、访问令牌、敏感信息不回显、治理拦截、审计日志、确认边界。
- Pro 价值要围绕“真实工作推进能力”:Shell、定时任务、监控、Skill、浏览器自动化、无限对话、优先支持。

## 体验原则

- 少让用户写工单。用户给模糊目标时,先自己读环境、找线索、提出可执行路径。
- 能动手就动手,但不可逆/高风险动作先确认。
- 默认交付结果,不要展示一堆过程噪音。过程可以记录在审计/工作台里,客户聊天区要干净。
- 客户看不见工具调用时,要有“正在思考/执行”的轻量反馈,避免以为系统卡住。
- 设置页必须像产品,不是环境变量堆砌。用户不懂的安全项要能一键配置或给出明确解释。
- 官网表达要先讲安全和本地化,再讲主动、记忆、工具和模板。

## 技术方向

### 1. 意图与角色识别

需要一个轻量的“语境判断层”,在进入执行前判断用户这句话属于:

- 问答解释
- 代码/文件审阅
- 修复实现
- 调研总结
- 决策建议
- 创作写作
- 任务执行
- 情绪/陪伴式梳理

它不一定要复杂模型,但要让 Captain 的第一反应更像“会判断场合的人”。

### 2. 计划与验收闭环

Cowork 模式要形成稳定闭环:

目标理解 -> 计划 -> 执行 -> 产物 -> 自检 -> 必要时返修 -> 汇报。

未来重点不是增加更多工具,而是提高每一步的完成率和验收质量。

### 3. 长期记忆分层

记忆至少分四类:

- 用户偏好:称呼、表达风格、工作习惯。
- 项目记忆:这个项目的目标、架构、关键决策、当前状态。
- 经验记忆:某类问题上次怎么解决、哪些坑踩过。
- 事实记忆:稳定事实,以及需要定期刷新验证的事实。

不要把所有东西混成一锅。记忆要可追溯、可删除、可解释。

### 4. 安全治理产品化

安全不能只存在于后端代码。用户应该能在 UI 里理解:

- 哪些数据在本机。
- 哪些操作会被拦截。
- 哪些操作需要确认。
- 远程访问令牌有什么用。
- 审计日志记录了什么。

安全是卖点,不是麻烦。

### 5. 评测体系

要建立一组“真实工作任务”评测,不只测接口:

- 模糊需求识别是否正确。
- 是否会主动读必要文件。
- 是否会少问废问题。
- 是否能产出可用文件。
- 是否会自检和修复。
- 是否能在危险操作前停下来。

这会比单纯堆功能更接近 AGI 方向。

## 我对下一阶段的建议

优先级按“靠近可靠协作者”的价值排序:

1. **建立语境判断层**
   给每轮用户输入先做 intent/role 分类,决定是顾问、审阅者、执行者、研究员、产品经理还是安全官。

2. **重做任务开始前的简短计划体验**
   不要每次都长篇列计划。复杂任务才展示计划;简单任务直接做。计划要能被执行进度复用。

3. **强化交付自检**
   所有生成产物都要回读关键内容;代码改动要跑最小测试;网页改动要做视觉/链接检查;不能只说“完成”。

4. **把项目记忆接入 UI**
   设置页或治理页可以展示“Captain 记住了什么”,让用户能编辑、删除、导出。

5. **安全能力前置展示**
   官网、启动检查、设置页都要把安全作为第一卖点:本地化、令牌、AUTH_SECRET、WORKSPACE_ROOT、审计日志、风险确认。

6. **客户交付路径打磨**
   Windows 安装、更新、授权码、备份迁移、恢复、日志诊断都要做到“客户按说明能自己走完”。

7. **建立长期路线图文档**
   把“可靠推进”拆成季度目标,每次开发都能对照:这个功能是在靠近北极星,还是只是多一个按钮。

## 重要约定

- 不要把用户给过的 GitHub token、邮箱授权码、模型 key 写入文档或提交历史。
- `.env`、`logs/`、`data/` 里的用户隐私默认不进 git。
- 推送 GitHub 前先跑至少:
  - `git diff --check`
  - `node --check frontend/app.js && node --check frontend/app.boot.js`
  - `python3 -m pytest -q`
- 如果改了 `.github/workflows/*`,GitHub token 需要 `workflow` 权限。
- 当前用户希望这个项目长期向“可靠的 AGI-like 私人协作者”靠近。做取舍时,优先选能提升理解、推进、记忆、边界和信任的方案。

