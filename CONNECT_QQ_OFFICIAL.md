# 把 QQ 官方机器人接到 Captain(稳定 · 免公网)

和 NapCat 不同,这是**腾讯官方机器人**:不会掉线、不会封号。代价是它是一个**独立的机器人身份**(不是你的个人号 1026839274),别人要在 **QQ 频道 / 群**里加它、@它来用。

走的是 **WebSocket 网关模式**——机器人主动连腾讯,**不需要公网地址、不用内网穿透**。

## 第 1 步:在 q.qq.com 建一个机器人(只有这步必须你来)

1. 打开 https://q.qq.com ,用 QQ 登录,进「机器人」管理端。
2. 新建一个机器人。个人开发者通常可建 **频道机器人**;群/单聊消息能力可能需要企业认证(看页面提示)。
3. 建好后,在「开发设置」里拿到两样:
   - **AppID**
   - **AppSecret**(机器人密钥)

> 拿不到群/C2C 能力也没关系——频道(QQ 频道)里 @机器人 就能用,先跑通再说。

## 第 2 步:填进 .env(我已经帮你留好位置)

打开项目根的 `.env`,把这两行填上(其余我已配好):

```
QQ_BOT_APP_ID=你的AppID
QQ_BOT_SECRET=你的AppSecret
```

## 第 3 步:启动

```
lsof -ti tcp:8000 | xargs kill -9    # 先清掉可能占着端口的旧进程
cd ~/Desktop/"my agent" && .venv/bin/myagent-web
```

启动后日志里看到这两行就算通了:

```
[server] QQ(官方机器人·WS 网关)启动中,无需公网回调
[qq] 已连上腾讯网关:wss://...
[qq] 网关握手完成,开始收消息
```

然后把机器人加进一个 **QQ 频道**,在频道里 **@机器人 说句话**,Captain 就会回。

## 说明

- **不掉线**:网关断了会自动重连;心跳保活由程序处理,你不用管。
- 想用回调(webhook)模式,把 `.env` 里 `QQ_BOT_MODE` 改成 `webhook`(需要公网地址,一般用不上)。
- 收不到消息常见原因:机器人没被加进频道/群,或没 @它;群/C2C 能力未在 q.qq.com 开通(改用频道测试)。
- 高危/花钱操作,Captain 会发「需要确认」,你回 `y 确认码` 放行。
