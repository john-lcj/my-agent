# Agent 嵌入工作流设计

目标：让 Captain 像 Claude Tag / 项目标签一样嵌入用户的真实工作流。用户不是每次从空白聊天开始，而是在某个业务上下文、文件、客户、项目或固定流程里唤起 Captain。

## 第一阶段：Markdown 工作流卡片

每个工作流先用一个 Markdown 文件表达，放在项目或用户工作区中。Captain 读取后把它当成任务上下文、执行边界和验收标准。

建议结构：

```md
# 工作流名称

## 适用场景
这个工作流什么时候被调用。

## 用户目标
用户最终想要什么结果。

## 输入
- 需要哪些文件、URL、客户资料或上下文
- 哪些输入是必填，哪些可以由 Captain 自动发现

## Captain 角色
这次 Captain 应该扮演执行者、研究员、审阅者、产品经理、安全官等哪类角色。

## 执行步骤
1. 目标澄清
2. 资料读取
3. 拆解计划
4. 执行
5. 自检
6. 返修
7. 汇报

## 安全边界
- 允许自动读取哪些目录
- 哪些动作必须确认
- 禁止访问或外发哪些内容

## 输出
- 最终产物路径
- 汇报格式
- 是否需要邮件/导出/归档

## 验收标准
- 用户如何判断完成
- Captain 自检需要覆盖哪些点
```

## 第二阶段：工作流注册表

把 `workflows/*.md` 注册到 Captain 的“工作流模板”页面：

- 列表展示：名称、场景、最近使用时间
- 点击调用：把 Markdown 内容注入当前会话的 system/workflow context
- 支持绑定项目：某个项目默认带某些工作流
- 支持快捷命令：`/workflow 客户周报`

## 第三阶段：执行闭环

每次调用工作流后，Captain 都走统一闭环：

1. 语境判断：判断这轮该当顾问、审阅者、执行者、研究员、产品经理还是安全官
2. 目标确认：只在影响方向时提问
3. 计划生成：列出可执行步骤
4. 执行记录：每一步写入任务进度和审计日志
5. 自检：对照工作流验收标准
6. 返修：发现缺口自动补一轮
7. 汇报：输出结果、产物、风险和下一步

## 第四阶段：产品化入口

前端可以增加一个“工作流”入口：

- 新建工作流
- 从 Markdown 导入工作流
- 绑定到项目
- 一键运行
- 查看历史运行记录
- 从一次成功任务反向沉淀成工作流

## 初始内置工作流建议

- 客户交付报告
- 官网/落地页更新
- 代码审阅与修复
- 数据整理与 Excel 报表
- 安全检查与诊断包
- 产品需求拆解
- 微信公众号文章生产

## 当前约定

在未做 UI 前，先把工作流 Markdown 作为项目资料保存。后续 Captain 读取 `AGENT_EMBEDDED_WORKFLOW.md` 和具体 `workflows/*.md`，即可理解我们要把工作流做成“可复用、可审计、可迭代”的 Agent 操作单元。
